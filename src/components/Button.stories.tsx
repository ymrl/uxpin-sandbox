import * as React from 'react';
import { Button } from './Button';

export default {
  title: 'Components/Button',
};

// export const Default =({children = 'button'}) => <Button>{children}</Button>
export const Hoge = () => <Button>hoge</Button>;
